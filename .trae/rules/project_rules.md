# 项目规则

## 构建命令

- 本项目进行 Hugo 构建时，统一使用：
  - `/Volumes/mini_matrix/github/a1pha3/web/hugo/hugo`

## 校验约定

- 进行站点构建验证时，优先使用：
  - `/Volumes/mini_matrix/github/a1pha3/web/hugo/hugo --minify`

## Python 命令约定

- 运行 Python 命令时，命令前必须加 `uv run`
